%FILE%
=============================

Context
=============================
%HERE%

Resolution
=============================
Run the following code in Rainbow

```ruby

PREFIX = "prefix"

Client.in_client(url_prefix: PREFIX) do
  ActiveRecord::Base.transaction do
    count = 0
    
    puts "Commit #{count} changes? (y)"
    raise "No changes committed" unless gets.chomp == "y"
  end
end
```

Asana Tasks
=============================
- 

